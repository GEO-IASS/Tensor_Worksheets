function value = operator_bracket(T,I)
n = size(T); % mode sizes
d = length(n); % dimension

% [...] % first calculate  pos  and then call  value = T(pos);

value = 'ANSWER 3 MISSING'; % delete this line one you are finished
end