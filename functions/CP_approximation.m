function Rho = CP_approximation(T,r,max_iter)
n = size(T);
d = length(n);
Rho = cell(d,1);

% [...]

for iter = 1:max_iter

    % [...]

    for mu = 1:d % one sweep

        % [...]

    end

Rho = 'ANSWER 5 MISSING'; 
end

